# Model 3D

Letak fail model 3D ringan (`.gltf` / `.glb`) di sini untuk gantikan placeholder kotak oren.

Rujuk `data/nouns.json` — set `model.type` kepada `"gltf"` dan `model.url` kepada path fail di sini (contoh: `assets/models/tiger.glb`).
